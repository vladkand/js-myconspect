//  ! ПСЕВДОСЛ. ЧИСЛА
// * Math.random();
// получаем случ. число
/* 
console.log(Math.random());
 */
// ==========================

// случайное число в пределах от min to max
// 1. определяем max и min
/* 
const max = 6,
  min = 1;
// получаем случ. число Math.random
let cubeOne = (Math.random() * (max - min) + min);
let cubeTwo = (Math.random() * (max - min) + min);
// выводим 
console.log(cubeOne, cubeTwo);
// округляем с поиощюь метода Math.round и выводим
console.log(Math.round(cubeOne));
console.log(Math.round(cubeTwo));
 */

  // console.log(Number.parseInt(cubeOne.toFixed(0)));
  // console.log(Number.parseInt(cubeTwo.toFixed(0)));

